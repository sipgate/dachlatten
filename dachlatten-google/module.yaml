product:
  type: lib
  platforms: [android]

dependencies:
  - $libs.kotlinx.coroutines.play.services: compile-only

test-dependencies:
  - $libs.kotlinx.coroutines.play.services

settings:
  publishing:
    group: de.sipgate
    name: dachlatten-google

apply:
    - ../common/kotlin.yaml
    - ../common/coroutines.yaml
    - ../common/kover.yaml
    - ../common/publishing.yaml
