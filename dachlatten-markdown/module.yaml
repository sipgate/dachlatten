product:
  type: lib
  platforms: [android, jvm]

dependencies:
    - $compose.foundation
    - $compose.ui
    - $compose.material3
    - $libs.jetbrains.markdown

dependencies@jvm:
    - $compose.desktop.currentOs

test-dependencies@android:
    - $libs.androidx.compose.ui.test.junit4
    - $libs.androidx.compose.ui.test.manifest

settings:
    compose: enabled
    publishing:
        group: de.sipgate
        name: dachlatten-markdown

apply:
    - ../common/kotlin.yaml
    - ../common/kover.yaml
    - ../common/publishing.yaml
